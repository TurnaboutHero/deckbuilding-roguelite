# Phase 1 보류 원장

> 기록일: 2026-07-10 · 갱신: 2026-07-11 (오너 오버라이드)  
> 결정: Phase 1의 코드 구현·자동 검증·배포는 완료 상태를 유지한다. 아래 항목은 **통과로 간주하지 않고 보류**하되, Phase 2(M5~M6) 자동 개발의 선행 차단 조건으로 사용하지 않는다.

## 오너 오버라이드 (2026-07-11) — 공학 트랙 진행권

제품 오너가 사람 게이트를 **개발 선행 조건에서 제거**했다 — 오너 지시는 2026-07-11 세션에서
전달됐고 이 원장 기록(커밋 `7fbd9b7`)이 그 결정의 영구 기록이다. 이것은 게이트 통과가 아니다 —
아래 원칙으로 기록·유지한다:

- 사람 게이트는 절대 `passed`로 표기하지 않는다. 차단 효력만 `waived-for-progression`으로 바꾸고 검증 상태는 `experience-unverified`로 남긴다.
- 모든 공학 결론은 `engineering-safe / balance-provisional / experience-unverified` 3축으로 구분한다.
- 수치·확률 변경은 결정론 시뮬레이션·지배 분석·콤보 상한·명시적 규칙 모순이 방향을 특정할 때만 허용하며 전부 provisional이다.
- 사람 반응이 실제로 수집되면 이 원장의 게이트 행이 최종 판정으로 복귀한다.
- 증거 계약: `PRD/PHASE3_EVIDENCE_CONTRACT.md`.

## 사람 검증 게이트

> 실행 수단은 `PRD/PLAYTEST_KIT.md`(테스터 안내·설문·집계 절차)와 `pnpm sim:human`(로그 집계)이다.
> 도구·픽스처 검증(N=5 결정론 픽스처)은 **사람 증거가 아니며**, 아래 상태를 바꾸지 않는다.

| 항목 | 현재 상태 | 닫힘 조건 |
|------|-----------|-----------|
| M2 손맛 게이트 | **experience-unverified** (차단 효력 waived 2026-07-11 — 사람 대기 유지) | 정식 Pages 빌드로 3~5명이 플레이하고, 카드 선택·코인 투입·플립 결과의 이해도와 조작감을 기록한다. |
| M4 자원 갈등 게이트 | **experience-unverified** (차단 효력 waived 2026-07-11 — 사람 대기 유지) | 같은 빌드에서 코인을 플립할지 스킬 비용으로 소비할지 고민한 순간과 선택 이유를 관찰·기록한다. |
| M5 체감 + M6 사람 N≥5 지표 | **experience-unverified** (차단 효력 waived 2026-07-11 — 사람 대기 유지) | 실제 사람 플레이 로그 N≥5를 `sim:human`으로 집계하고 §8.3 지표·정성 설문으로 판정한다. |
| Phase 3 진입 | **waived-for-progression** (2026-07-11 오너 오버라이드 — 공학 게이트로 대체, 체험 검증은 미완) | 사람 게이트가 실제 사람 증거로 닫히면 이 행을 최종 판정으로 갱신한다. |

## 폴리시·UX 보류

| 항목 | 현재 상태 | 닫힘 조건 |
|------|-----------|-----------|
| 거부 행동 사유 피드백 | **닫힘 (2026-07-11, `41bab3e`)** | 한국어 사유 9종 카탈로그(`apps/ui/src/action-feedback.ts`) + aria-live 칩, 단위 테스트와 브라우저 테스트(`apps/ui/scripts/feedback-check.mjs`)로 고정. |
| 프리뷰의 자해·코인 생성 축 | **닫힘 (2026-07-11, `41bab3e`)** | `previewFlip` byAxis/expected에 selfDamage·coinsCreated 추가, 코어 테스트 + 브라우저 테스트(화염 폭주 자해 0~2·생성 0~1, 베기 무표시)로 고정. |
| 소비 코인 직접 선택 규칙 | **닫힘 (2026-07-11, `7ea1a17`)** | 규칙 확정·구현: count 1 = A15 자동 유지, count≥2 = 연료 지정 모드(자동 제안 사전 선택·토글·Escape/타행동 취소), 실행 `coins[]` 손패 순서 결정론 — `apps/ui/src/fuel-selection.ts` + test-db 단위/통합 테스트. 브라우저 E2E는 첫 cost≥2 콘텐츠 PR의 필수 게이트 (`PRD/PHASE3_PREP.md` §4). |

## 검증 증거 보류

| 항목 | 현재 상태 | 닫힘 조건 |
|------|-----------|-----------|
| URL 시드 재현성 | **닫힘 (2026-07-11, `41bab3e`)** | playtest 시나리오 13 — 같은 시드 URL 2회 신규 부팅의 DOM 지문(시드·주머니·손패·적 HP/의도·카드 상태) 동일성 검증, 로컬·CI 양쪽 green. |
| Playwright CI 통합 | **닫힘 (2026-07-11, `41bab3e`)** | CI `playtest` 잡 추가(chromium 설치+빌드+전 시나리오 260어서션, 실패 시 스크린샷 아티팩트). green 실행: https://github.com/TurnaboutHero/deckbuilding-roguelite/actions/runs/29110395064 (playtest: success). |

## 유지하는 MVP 가정

- 사운드는 MVP 범위에서 제외한다.
- 한국어 단일 언어로 제공한다.
- 최신 데스크톱 브라우저를 우선 지원한다.

## 운영 규칙

- 위 항목은 닫힘 증거가 생기기 전까지 `완료` 또는 `통과`로 바꾸지 않는다.
- Phase 2 구현 중 관련 코드를 건드리더라도 이 원장의 범위를 몰래 확장하거나 판정을 대신하지 않는다.
- M6 사람 지표 게이트와 Phase 3 진입 판정은 이 예외에 포함되지 않는다. M6 자동 지표와 사람 표본을 확인한 뒤 별도로 결정한다.
