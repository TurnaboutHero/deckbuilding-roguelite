## 골 검토 요약 (Step 8 자동 생성)

- 목표: Phase 1 사람 보류를 유지한 채 Phase 2 M5 런 루프와 M6 밸런스 자동화 완성
- 마일스톤: M5 5연전·보상·저장 / M6 정책 4종·지표·CRN·CI smoke·로컬 텔레메트리
- 필수 검증: typecheck · lint · test · build · seed42 골든 · `ci:sim` · `sim:balance`
- scope 잠금: 사람 게이트 자동 통과 금지, 밸런스 밴드 CI 게이트화 금지, Phase 3 확장 금지

---

# PROGRESS

## 현재 골

Phase 1 보류 항목을 명시적으로 유지한 채 Phase 2를 진행한다. M5에서 5연전 런·보상·저장 루프를 완성하고, M6에서 정책 4종·밸런스 지표·CRN A/B·로컬 텔레메트리를 구축한다.

## 현재 마일스톤

**✅ P11 냉기 도적 동기화 완료 / 사람 검증 대기 (2026-07-13)** — 냉기 도적의 보존·지정 뽑기·가변 소비 엔진과 전체 스킬/패시브 데이터를 반영했다. 턴 종료 보존은 손패·장전 동전을 직접 고르고 버튼 재클릭/Enter로 확정하는 UI까지 구현했다. 혈액 캐릭터는 전용 기획 원본 확인 전까지 보류하며, 다음 단계는 지정 뽑기 선택 UX와 사람 플레이 기반 냉기 밸런스·손맛 판정이다.

## 완료

- [x] **P11 냉기 도적 기획 동기화** (2026-07-13, Codex 구현·검증, Fable 추가 사용 없음) — 저장 호환을 위해 ID `frost-knight`를 유지한 채 표시명과 시작 세트를 냉기 도적으로 교체했다. `이중 주머니` 보존(기본 1·추가 턴당 2·동시 3), 실제 동전 지정 뽑기, 냉기 동전 일부/전부 소비, 소비량·동상 비례 피해, 보존 기본 동전의 제한적 냉기 취급을 엔진에 추가했다. 시작 스킬 2종과 빌드 스킬 10종(전용 총 12종), 전용 패시브 8종 및 강화안을 콘텐츠 `1.5.0-p11`에 반영했다. 턴 종료 시 손패와 장전 동전의 보존 후보를 직접 토글하고 버튼 재클릭/Enter로 확정하며 Escape로 취소한다. 자동 진행만 합법 후보의 결정론 제안을 유지한다. 구기획 `동장군`은 현 보상 풀에서 제거하고 P10 저장 ID만 `서리털 망토`로 마이그레이션한다. 혈액 캐릭터는 확인 가능한 전용 기획 원본이 없어 후속 확인까지 보류했다. 정본: `P11_COLD_ROGUE_DESIGN_SYNC.md`.

- [x] **P8.4 피드백 영구 회귀 증거** (2026-07-13, Codex 직접 구현·검증, Fable 미사용) — ① `pnpm check:feedback-regression` 단일 명령으로 1280×720 키보드 툴팁과 390×844 터치 툴팁의 포털·8px 뷰포트 경계·최상단 hit-test·`aria-describedby`·Escape 계약을 검증한다. ② 실제 기본 공격을 사용해 적 유닛 DOM의 `vfx-hit`과 가시 피해 텍스트를 함께 확인하고, reduced-motion에서는 이동 큐만 제거한 채 의미 정보가 유지됨을 고정했다. ③ 부팅 전 fake Web Audio로 기본 음소거가 AudioContext를 만들지 않음, 키보드 토글·localStorage 영속, 동전 장전 1이 단일 mapped cue를 정확히 한 번 시작함, 실제 공격이 추가 음향 활동을 만듦을 검증한다. 이 과정에서 P8.3 매퍼에 정의됐지만 큐에서 제외돼 재생되지 않던 `coinPlaced`/`coinUnplaced` VFX·SFX 결함을 발견해 즉시 이벤트 경로로 복구했다. ④ 마도기사 소환 슬롯과 지속 시간 접근성 라벨, 콘솔·페이지·4xx 오류 0을 확인한다. ⑤ `docs/ui/regression/p8/`에 고정명 PNG 4개와 UTF-8 `manifest.json`을 생성하며 이미지 decode+2 RAF 뒤 캡처해 합성 레이스를 제거했다. focused run·UI typecheck·lint·build 통과, 시각 판정 **94/pass**. CI playtest, 수동 배포 verify, 태그 release verify와 root `release:verify`에 차단 게이트로 연결했다. 수치·확률·콘텐츠·외부 자산·의존성 변경은 없다. **P8 engineering-complete / audio-taste-and-feel-unverified**.
- [x] **P8.3 합성 SFX 확장** (2026-07-13, Codex 직접 구현·검증, Fable 미사용) — ① 순수·전수 `sfxCuesFor` 매퍼로 `CombatEvent`와 음향 팔레트를 분리하고 기존 플립·피해·방어 분기 재생을 단일 이벤트 경로로 통합했다. ② 동전 장전/회수/생성/소비/셔플, 스킬, 쿨타임 복귀, 화염/마나/냉기/전기/혈액, 과열 진입/소비, 소환 생성/행동/교체/소멸 구분음을 Web Audio 오실레이터+엔벨로프로 추가했다. ③ 반복 피로를 줄이는 UI 전용 ±2% 주파수 변주를 추가했으며 코어 상태·리플레이·텔레메트리는 건드리지 않는다. ④ 기본 음소거·설정 영속·AudioContext 부재/생성/재생 실패 무음 폴백을 유지하고, 전 팔레트 25종이 무예외 호출되는 회귀 테스트를 고정했다. 외부 음원·신규 패키지·네트워크 로딩은 0이다. ⑤ 신규 매퍼 3건 포함 48파일 466/466 tests(Windows human CLI wrapper 3건 제외), typecheck 4/4·lint·build·provenance·contrast 통과. 합성 코드 증가분을 위해 총량 예산만 +4KiB로 명시 조정하고 JS 400KiB·CSS 70KiB·단일 파일 700KiB 한도는 유지했다. **engineering-complete / audio-taste-unverified**.
- [x] **P8.2 전투 피드백 VFX** (2026-07-13, Codex 직접 구현·검증, Fable 미사용) — ① `CombatEvent` 전체를 순수·전수 `feedbackCuesFor` 매퍼로 투영해 코어 결정론·이벤트 타이밍과 시각 연출을 분리했다. ② 피해·방어 기존 피드백을 매퍼로 통합하고 회복·화상/동상/감전·위축·과열 진입/소비·쿨타임 감소·스킬 사용·소환 추가/교체/행동의 대상별 피드백을 추가했다. ③ 상태 float의 고정 `화상` 문구를 실제 `statusKo` 결과로 교정했다. ④ 정보성이 낮은 전장 중앙 범용 링은 시각 검수 후 제거하고 실제 변화 대상만 맥동하도록 단순화했으며, 기존 `vfx-block-pop`을 재사용해 신규 CSS·자산·의존성 없이 번들 예산을 지켰다. `prefers-reduced-motion`에서는 신규 큐를 생성하지 않는다. ⑤ 신규 매퍼 테스트 3건, typecheck 4/4·lint·47파일 463/463 tests(Windows 확장자 없는 `.bin/tsx` 제약의 human CLI 3건 제외)·build·provenance·contrast·budget(총 2,740,280B·JS 388,660B·CSS 71,645B)·Playwright S1~S36 전 항목 통과, 시각 판정 93. 로컬 perf는 TTI/LCP/CLS 통과, 머신 변동성 롱태스크 126~127ms 2건을 보고 전용으로 남겨 Linux CI에서 재판정한다. **engineering-complete / experience-unverified**.
- [x] **P8.1 오버레이·툴팁 레이어 복구** (2026-07-13, Codex 직접 구현·검증, Fable 미사용) — ① `#overlay-root`와 7단계 레이어 토큰(world/stage/controls/popover/tooltip/drag/modal/notice)을 단일 계약으로 도입하고, 중첩 `z-index`로 해결할 수 없던 `overflow:hidden`·로컬 stacking context 문제를 포털로 제거했다. ② `AnchoredOverlay`는 앵커 중심 정렬, 상단 우선/하단 반전, 8px 뷰포트 클램프, resize·scroll·콘텐츠 크기 변화 재계산을 지원한다. ③ 카드 키워드·턴 버프·몬스터 패시브 툴팁, 스킬 프리뷰, 뽑기/버림/소모 더미, 드래그 프록시, 런 메뉴를 각각 tooltip/popover/drag/modal 계층으로 이전했으며 hover·focus·touch·Escape·바깥 클릭 계약을 유지했다. ④ 순수 배치 단위 테스트 5건, 빠른 브라우저 내비게이션 계약, 전체 Playwright S1~S36, 1024×720·1280×720·1920×1080·390×844·844×390 전 항목 통과. 턴 버프·패시브 툴팁은 `insideViewport=true`·`layer=tooltip`·`topmost=true`를 실제 DOM 기하로 검증했다. ⑤ typecheck 4/4·lint·build·ci:sim 500/500 terminal/crash0/invariant0/seed42 exact·provenance·contrast·budget(총 2,738,746B, P8.1 인프라 예산 +4KiB)·perf(LCP 216ms·CLS 0.00095·100ms 초과 long task 0) 통과, 시각 판정 94. Windows 로컬의 확장자 없는 `.bin/tsx` 직접 실행 제약으로 human CLI 3건만 실패하고 나머지 Vitest 461건 통과 — 전체 464건은 Linux CI에서 최종 확인한다. **engineering-complete / experience-unverified**.
- [x] **런 내비게이션·캐릭터 선택 UX** (2026-07-13) — 저장 유무를 명시하는 타이틀 화면(이어하기/새 런), 전투·보상·노드 전 화면에서 여는 런 메뉴(계속/마지막 저장 불러오기/새 런/타이틀), 파괴적 동작 확인 단계를 구현했다. 새 런은 항상 5종 캐릭터 선택을 거치며 기존 idle 아틀라스를 카드 초상으로 재사용한다. `?seed`·전투 픽스처 직접 진입은 저장이 없는 테스트 경로에서만 유지해 일반 새로고침이 저장 런을 초기화하지 않도록 분리했다. 데스크톱/모바일 브라우저 계약 16항목(불러오기 세션 교체 포함), 타입·lint·build, 자산 예산(총 2.609MiB·JS 384,210B·CSS 71,575B) 통과; 시각 판정 94. Windows 로컬 CLI 셸 경로 3건을 제외한 Vitest 455건 통과, 전체 459건은 Linux CI에서 최종 확인한다.
- [x] **P7 프로토타입 피드백 통합 — 무행동캡·쿨다운·8슬롯·양면 속성·과열/드로우 (phase7)** (2026-07-12, Fable 초안 + Codex 적대 감사·보완) — 정본 `PRD/P7_NEW_DESIGN_DECISIONS.md`(D1~D10). ① **행동 모델(D1)**: 턴당 3회 카운터 완전 삭제, 기본기 쿨다운0 반복, 일반 스킬 쿨다운1~3, 전투당1회 단일 잠금, 턴 시작 감소, 자기/반복/전투당1회를 제외한 쿨다운 감소 원자. ② **빌드 공간(D2)**: `MAX_SKILL_SLOTS=8`, 전 캐릭터 시작 4스킬(기본 공격·방어+전용2), 빈 슬롯 `null`, save v7 및 v1→v7 마이그레이션·빈 슬롯 강화 불가 불변식. ③ **고코스트 지원(D3)**: 즉시/다음 턴 드로우, 손·뽑기·버림 임시 코인 생성 강도 계층, 4~5코스트 리턴 규칙, 재정비/전투 집중/병기 난사 등 대표 콘텐츠. ④ **양면 속성(D4)**: 화염(앞 화상/뒤 피해), 마나(앞1·뒤2 방어), 냉기(앞 동상/뒤 방어), 전기(앞 감전/뒤 피해), 혈액(앞 회복/뒤 방어); 소비는 무플립·무proc 유지. 단일/전체/자기 스킬의 적대 proc 대상 규칙을 엄격화하고 전체 대상은 스킬 본체·면·proc 모두 생존 적 전원 적용. 미지정/사망 대상 거부는 코인·쿨다운·과열 상태 불변. ⑤ **과열(D5)**: 화염 1소비 `내면의 발화`로 진입+즉시 드로우, 중첩 없음·턴 지속·강화 스킬 성공 해결 뒤 해제, 화염 정권/혜성권 등 과열 분기; 소비 스킬 면 보너스 충돌은 전역 무플립 규칙 우선으로 명시. ⑥ **UX/자산(D6~D8)**: 반복/쿨다운/전투당1회/과열 카드 배지와 툴팁, 점진 튜토리얼 5종(localStorage 별도 영속), 신규 카드 아트 6종(격투가 주먹·마도기사 병기 문법, 프롬프트 검증 증거), 빈 슬롯 검 실루엣→불씨 폴백. ⑦ **Codex 감사 수정**: oncePerCombat 쿨다운0 정규화, self/none 공격형 속성 proc의 명시 대상 강제, 진짜 AoE fan-out, reduceCooldown 비대상 구조화, 치명타여도 임시 코인 생성 순서 보장, save v7 null-slot upgrade 위조 거부, 브라우저 픽스처 전면 v7화. ⑧ **검증**: typecheck·lint·46파일 **459/459 tests**·build·provenance·대비12항목·모바일/데스크톱 실제 브라우저 **전 항목 PASS**, perf(총 2,726,281B·JS377,609B·CSS68,684B·LCP420ms·CLS0.00013·100ms 초과 long task 0), 시각 판정 **93**, ci:sim 500/500 terminal·crash0·invariant0·seed42 golden exact. **engineering-complete / balance-provisional / experience-unverified** — 정책별 승률 편향(turtle 123/125, aggro 0/125)은 사람 플레이·봇 정책 개선 전 수치 조정 근거로 사용하지 않는다.
- [x] **P6 신규 기획 대통합 — 3막 런·패시브/강화·화염 격투가·마도기사 (phase6)** (2026-07-12~13, Fable 오케스트레이션 + 서브에이전트 3기 + Codex 시각 감사 반영) — 정본 `PRD/P6_NEW_DESIGN_DECISIONS.md`(D1~D9), 증거 `PRD/P6_BALANCE_EVIDENCE.md`. ① **3막 런(D1)**: 3막×10방문(1~8 후보 2~3, 9 휴식, 10 보스), 생성 분포 50/10/2/15/3/20(생성/방문 분리 텔레메트리), 막 스케일 ×1.15/1.3, 막 보스 3종 재사용 배치, 막 보스 클리어 전체 회복(누적 소모 산술 근거 D9), save v6(v5=레거시 막 래핑, v1→v6 체인), 보상 신스펙(일반=동전3택/엘리트=+스킬1/보스=+패시브3택, 제거는 상점 회귀). ② **성장 기반(D2·D3)**: 획득 패시브 풀 12종(innate와 데이터·표시 구분, 중복 불가, 보물/보스/상점 출처, 안전 원자 검증), 스킬 강화 5종 patch+오버레이(리플레이 무변경), 휴식 회복30%|강화 택1. ③ **화염 격투가(D5)**: warrior 표시명 전환+격투 ID 3종 가산(세이브 최안전), 스킬 10종(화상 비소비 폭발 damagePerTargetBurn·과열 damagePerFireInHand 패키지 — 태세/집중은 백로그), 전용 패시브 8(무속성5+화염3), 검술 문구 격투 전환. ④ **마도기사(D6)**: 신규 arcanist(guardian 무접촉) — 소환 엔진(3슬롯·선입 자동 행동·지속·최고령 교체·재타깃·이벤트 4종 결정론), turnStart trait 마도 공방, 마력 갑주(damagePerBlock)/마나 병기(empower) 빌드, 시작 스킬 마력 충전·명령(뒷면 보너스는 flip tailsCount 컨텍스트). ⑤ **UI(D7)**: 막 진행 표기·휴식/보물 화면·보스 패시브 3택·상점 패시브·소환 레일·강화 ＋배지·패시브 배지(전부 모바일/forced-colors/reduced-motion 계약 유지). ⑥ **자산(D7)**: 격투가(그린 키 핀·성인 여성 톰보이)·마도기사(마젠타·아카데미 제복, v1 분리 마법진 FAIL→재생성) 스프라이트 — 킷 정련 전량 ok=true·0E, base lock, component-row, 모션 QA 3상태 PASS, exact-palette 무손실, 시각 판정 92/91; Codex 시각 감사로 격투가·마도기사 카드 아트 18종 생성(주먹/건틀릿·소환 병기 어휘, 검 실루엣 제거). ⑦ **검증(D8)**: 테스트 422/422, playtest 493 [ok](S33 휴식·S34 보물·S35 보스 패시브·S36 소환 레일 신설), 휴먼 텔레메트리 v3(경로 사실 가산·v2 레거시 수용·v1 거부), ci:sim 500런 안전 게이트 green + turtle 11/125 완주(공학 최소 충족), 빌드 비교 스윕(guardian 28% 지배·arcanist 진행 최상/마무리 0·warrior 봇 최약=셋업 의존 해석·frost 봇 스톨메이트 관측 한계 기록), budget(JS 400KiB 상향 D9)/provenance/contrast/perf 게이트 PASS. engineering-safe / balance-provisional / experience-unverified
- [x] **P5.5+P5.6 릴리스 파이프라인·최종 마감 (phase5-release)** (2026-07-12, Fable 직접 + 워치 감사 3라운드 전부 반영) — ① **차단 게이트 2종 신설**: `check-provenance.mjs`(스프라이트 run 필수 파일·리포트 ok=true·프롬프트 킷 정련본 ok=true·0E — P3.2 사후 검증 실패 원본은 역사 증거로 구분 허용·오디오 파일 부재·카드 아트 킷 증거), `check-budget.mjs`(총 2.6MiB + **JS 320KiB + CSS 70KiB + 단일 파일 700KiB**). ② **JS 예산 무의존 충족(감사)**: terser 도입 시도를 AGENTS 의존성 계약 위반으로 롤백 — vite 내장 transform으로 스프라이트 manifest **런타임 계약 필드만 인라인**(provenance 필드는 소스 SSoT 유지) + es2022 타깃 + json.stringify + legalComments 제거 = 332,937→**318,028B**. ③ **perf 계약 차단(감사)**: LCP≤2500ms(median)·CLS≤0.1(worst)·>100ms 롱태스크 0 — warm-up 1회+3회 측정으로 변동 완화, 계약 무약화. 측정 LCP 284ms·CLS 0.0001·롱태스크 0. TTI·커맨드 라운드트립은 report-only(연출 포함 측정 재정의 유보). ④ **파이프라인 강화(감사)**: Node22·pnpm 9.15.4 고정·--frozen-lockfile·최소 permissions·concurrency·job timeout, **Deploy는 CI 성공 workflow_run의 exact head_sha만**(수동 dispatch는 입력 ref에 동일 게이트 전체 통과 후), Release(v* 태그)는 태그 SHA 전체 게이트→Pages 배포→성공 후 GitHub Release. root package: packageManager/engines + check:content/assets/a11y/mobile/perf·release:verify 표면. ⑤ **1.0.0-rc.1 승격 — 콘텐츠 실변경 포함**: 유일한 콘텐츠 변경은 ⑥의 구울 패시브. seed42 골든 재고정(turnsPerCombat [4,4,4,9,7,5]→[4,4,4,9,**8,2**] — 구울 포함 전투 연장·보스 조기 패배, **결말 defeat·조우 순서·최종 가방/스킬·불변식 전부 동일**, turns 변화는 balance-provisional 관측치). 0.10.0-p4.4는 LEGACY 편입 — 저장은 적 정의를 직렬화하지 않고 defId로 db를 참조하므로 `EnemyDef.passive`는 **스키마 가산(optional, 기본 undefined)**이라 구버전 저장이 그대로 호환된다. 타이틀 M0 제거, README 상태 절·PLAYTEST_KIT 현행화(모바일 지원·음소거·그래프 런). ⑥ **몬스터 패시브 수직 슬라이스(감사 지시)**: `EnemyPassiveDef` 계약(자기 대상 원자만 — 의도 비예고와 '매 턴 의도 공개' 계약 충돌 방지, 콘텐츠 검증 `validateEnemyPassives`) + 적 턴 시작 자동 발동 코어 + `enemyPassiveTriggered` CombatEvent + 전장 ★칩(hover/focus/touch 툴팁, Keyword entry 오버라이드) + 발동/회복 float + 단위 6종/콘텐츠 핀/E2E(S32). 구울 '썩은 육체'(자신 턴 시작 시 HP 1 회복, **balance-provisional**) — '포식' 회복 5와 의도적 공존: 패시브는 상시 미세 재생(§5.2 '화상으로 회복 상쇄' 압박 유지), 포식은 3턴 주기 순간 회복. 수치 확정은 사람 밸런스 판정까지 유보. 캐릭터 패시브(고유 특성)는 기존대로 코드·테스트·E2E 존재 확인. 검증: **406/406**·lint·typecheck·**ci:sim 재고정 green**(구울 패시브로 seed42 골든 재고정 — CI 29182950466 실측)·build·**playtest 458**(S32 10어서션 포함, step 분리 실측)·budget/provenance/perf/contrast 게이트 전부 exit 0. engineering-complete / experience-unverified
- [x] **P5.4 성능·복구 (phase5-recovery)** (2026-07-12, Fable 직접 + 감사 3건) — ① **저장 계약**: `saveRun` 주+백업 이중 쓰기(성공 여부 반환), `loadRunDetailed` 6상태 판별(missing/loaded/**recovered**/corrupt/unsupported/unavailable), 주 손상 → 백업 복구+주 키 복원, 주·백업 모두 무효 → **원문 격리 키 보존 후 명시 화면**(조용한 삭제 금지 — clearRun도 격리는 보존). 감사 수정: classifyInvalidRaw 데드 코드 제거 — 미지 콘텐츠 버전을 unsupported로 정확 판별(미래 스키마와 분리 테스트), unavailable → 저장 경고 배지 발행. 복구 계약 테스트 7건. ② 복구 UX: RecoveryBoundary(런타임 오류 재시도 — 백지 금지), 손상 저장 명시 화면(S30 — 자동 삭제 아님 검증), 저장 실패 경고 배지(`run-save-status` 이벤트 구독), 자산 onError 폴백(배경·**카드 아트·스프라이트 아틀라스** — 깨진 이미지 대신 조용한 제거, 진행 무차단). ③ **S31 리로드 영속 8페이즈 전부**(ready/combat[시도 증가 재개]/rewards/shop/event/choose-node/victory/defeat — 2중 리로드 검증, 17어서션). ④ 성능 계측 `perf-check.mjs`(report-only): TTI 233ms(예산 3s 내)·dist 2.53MiB(예산 내)·커맨드 왕복은 연출 포함 측정이라 지표 재정의 유보. 검증: 400/400·lint·build·playtest **448**·feedback-check·contrast 게이트 exit 0. engineering-safe / experience-unverified
- [x] **P5.3 접근성 (phase5-a11y)** (2026-07-12, Fable 직접 + 감사 2건) — ① **S29 키보드 완주 10어서션**: 전투(코인 선택→소켓 장전→스킬 사용→턴 종료 전부 focus+Enter)·상점(구매·나가기)·갈림길(선택)·이벤트(거절)·키워드 Escape — 초기 실패는 앱 결함이 아닌 하네스 대기 조건(locked 큐)으로 판명·수정. ② **대비 = 객관 증거 판정(감사)**: 스캐너의 그라디언트 배경 오탐(투명 backgroundColor→조상 폴백, 가짜 1.08)을 **최악-스톱 토큰쌍 보수 계산으로 수정·문서화** — 턴 종료 9.57:1로 오탐 증명, 카드 제목은 진짜 3.73:1 미달 → 잉크 #12101a로 **4.7:1 AA 통과**, 전 10항목 ok (`a11y-contrast.mjs`, report JSON). ③ 소스 레벨 6건: HP `role=progressbar`+valuenow/min/max, float-text `aria-hidden`(결산 티켓 라이브 리전 중복 낭독 방지), 공격 버프 칩 Keyword 툴팁(용어 사전 추가), `forced-colors: active` 지원(시스템 팔레트·경계 유지), **reduced-motion 시 JS 연출 딜레이 단축**(≤120ms — 모션의 일부로 취급), 다이얼로그 진입 포커스(primaryRef 기존 확인 — 화면 교체 의미론 문서화). 검증: 393/393·lint·build·playtest **428**·feedback-check. engineering-safe / experience-unverified
- [x] **P5.2 사운드 (phase5-audio)** (2026-07-12, Fable 직접 + 감사 1건) — ① **런타임 Web Audio 합성 SFX 7종**(flip 앞/뒤·타격·방어·구매·승리·패배 — 오실레이터+지수 엔벨로프): 오디오 자산 0바이트(번들 예산 무부담), 라이선스 리스크 0, provenance = 코드 자체(`audio.ts`). ② 기본 음소거(자동재생 정책 회피)·localStorage 영속·**전 경로 무음 폴백**(AudioContext 부재/생성 실패/재생 오류가 진행을 막지 않음 — P5.4 원칙 선반영). ③ 감사 반영: 전투 중복 토글 제거 → **RunMeta 단일 영속 컨트롤**(전투·상점·이벤트·보상 전 화면 도달), coarse 44px 타깃(저높이에선 음수 마진 흡수로 줄 높이 불변 — 가로 844×390 스크롤 0 재달성 sh=390), 정확히 1개·기본 음소거·aria-pressed·리로드 영속·**음소거 중 AudioContext 미생성** 테스트. 훅: 플립 결과(면별 음)·피해·방어·승/패·상점 구매. 검증: 393/393·lint·build·playtest **418**·feedback-check. engineering-safe / experience-unverified (음색 취향은 사람 게이트 유보)
- [x] **P5.1 모바일/터치 (phase5-mobile)** (2026-07-12, Fable 직접 + 감사 6건) — ① 반응형 2단(≤700px 세로·≤500px 저높이 가로): 유닛/플레이트/칩 축소, **카드 레일 flex-start 자체 가로 스크롤**(center 오버플로 클리핑 해소)·카드 고정높이(190px) 해제, 패널(상점·이벤트·갈림길·보상) **자체 세로 스크롤로 하단 제어 도달 보장**, 저높이에서 카드 아트/힌트 접힘·효과 행 기본 접힘(장전·포커스 시 펼침). 실측 반복 4회(자식 높이 계측)로 **세로 390×844·가로 844×390 페이지 스크롤 0** 달성. ② 감사 6건 반영: 100dvh+svh 폴백·env(safe-area-inset)·coarse 포인터 44×44 주요 타깃·터치 드래그 임계 6→12px·hover 전용 펼침 `(hover:hover) and (pointer:fine)` 게이트·자동 어서션. ③ 키워드 툴팁은 기존 onClick 토글로 터치 대응 확인. ④ S28 모바일 스위트 26어서션: 두 방향 × {전투(스크롤 0·터치 장전·턴 종료 뷰포트 내)·상점(나가기 도달)·이벤트(거절 도달)·보상(스킵 도달)} — 픽스처는 경제 보존 법칙 정합(엘리트 프리픽스). 검증: 390/390·lint·build·playtest **414**·feedback-check·시각 판정 **91**. engineering-safe / experience-unverified
- [x] **P4.6 적 스프라이트 5종·노드 아이콘·P4 마감 (phase4-assets)** (2026-07-12, Fable 직접 + 독립 시각 QA 교차) — ① 스프라이트 5종(도적·구울·마도사·슬라임·잿불 마도왕): 킷 선검증(base 5+행 15+재생성분 전부 ok=true·0E, `prompt-kit-validation/` 보존, skill-repos 기능 검증기), Base Lock 게이트(보스 1차 부유 파편 FAIL→재생성 재락, 독립 교차 판정), component-row 행 생성(SID 전량 provenance). 반려·재생성 이력: mage 전행 2세대(불꽃 분리·슬롯 근접)·boss hurt 3세대(임팩트 광선→불티→'방어 동작' 재프레임 클린)·slime hurt 2세대(진동 표시선) — 전부 **풀스케일 컴포넌트 4·파편 0 정량 검증**, 로컬 픽셀 수리 0. ② 파이프라인 결함 발견·수정: prepare의 chroma auto가 base 기준 cyan을 선택(행 배경은 마젠타) → 추출 전멸 원인, request 명시 마젠타 고정. ③ fit 미설정으로 아틀라스 565KB+(57k색) → **pixel-perfect fit(kCentroid·공유 팔레트) 재추출 + 정확 팔레트 P-mode 완전 무손실 변환(RGBA 바이트 동일)** — 5종 총 126KB, **raw 행 불가침**(D11). dist 2.40MiB(예산 2.6MiB 내). ④ UI: SPRITES 5종 등록·enemySprite 매핑(goblin 포함)·갈림길 노드 SVG 아이콘(emoji 금지 유지). ⑤ 모션 QA 5종 통과(idle 심리스·attack 3단·hurt 반동, 정체성 12프레임 일관). 검증: 390/390·build·playtest 388·feedback-check·시각 판정 **93**(신규 2적/3적/보스 전투·버프 칩 ↑공격 +8 노출·1024 무클리핑·RM). **P4 전체 마감** — 런 그래프·몬스터/보스·상점·이벤트·경제 MC·자산까지 D1~D11 완결. engineering-safe / balance-provisional / experience-unverified
- [x] **P4.5 경제 Monte Carlo (phase4-economy)** (2026-07-12, Pumasi 구현 1+Fable 수용·감사 4건) — ① `p4-economy-report-v1`(`sim:economy`, report-only·CI 비게이트): 캐릭터 4 × 노드 정책 2 × 500런 = 4,000런, terminal 100%·crash 0·invariant 0, `--games 5` 2회 바이트 결정론. ② 감사 반영 4건: 매복 EventTrace **정산 후 outcome 귀속**(수락 직후 0델타 오귀속 방지) / `--combat-policy` 파라미터화 + `combatPolicyMeaning: fixed-comparison-baseline`(D7 baseline은 캐릭터 최적 전략이 아님을 스키마에 명시) / seedRule 라벨 정정 `p45-<node-policy>-<character>-<index>`(시드 생성 불변 — 전투 정책 간 **짝지은 비교** 성립) / 파산률 3차 감사 — 문자적 D7 정의가 정책 구조상 8셀 전부 100% 포화(동어반복)로 실증되어 **지표 분리 판정**: `bankruptcyRate`(운영=무구매)+`unmetDemandRate`(문자적, 포화=상점 수요>골드 공급 구조 관측), 집중 테스트 3건. ③ **교차 진단**(4정책×4캐릭터×2노드×50런): aggro 고정 표의 guardian 외 전멸은 정책 부적합 교락으로 판명 — **최소 공학 기준 4캐릭터 전부 충족**(warrior turtle 28%/98%, guardian 100%/100%, sorcerer 34%/86%, frost-knight 74%/100%), turtle 전반 우세·guardian 지배 후보는 관측 플래그만(수치 무변경). ④ 전체 스윕 2회의 승률·보스 도달 8셀 비트 일치 = 짝지은 동일-시드 증거(3차 스윕 불요). 증거: `PRD/P45_ECONOMY_EVIDENCE.md`. 검증: 390/390·ci:sim 골든 불변·build·playtest 388·feedback-check green. engineering-safe / balance-provisional / experience-unverified
- [x] **P4.4 이벤트 시스템 (phase4-events)** (2026-07-12, Pumasi 구현 1+Fable 직접 검증·통합) — ① D10 이벤트 4종(공짜 금지 §6): 매복 현상금(엘리트 전투 → 승리 시 골드 70+**희귀 전용 2진열**)·피의 제물(HP 5, 하한 가드)·변환 제단(100골드, **영구 변환 — 이벤트 전용 PRD §10**, 기본 코인 직접 선택)·동전 희생(순 크기 불변 트레이드). 해결 결정론(주사위 없음)·전부 거절 가능. ② `derive(runSeed,'event-N')` 전용 스트림 — 기존 graph/combat/reward/shop 스트림 불변(수락/거절 무관 후속 전투·보상 동일 검증). 그래프 계획 v2(D1 이벤트 레이어 복원), CONTENT_VERSION **0.10.0-p4.4**·seed42 재고정 결속. ③ save v5(pendingEvent·pendingEventCombat·이벤트 카운터 3종) 체인 v1→…→v5, payload 계약(event 페이즈 전용·eventId 사전 대조·이벤트 노드 한정 pendingEventCombat), 보상 상한에 eventCombats 합산. ④ telemetry path additive variant(event accept/decline·choice) — 리플레이는 기록 사실로만 통과. ⑤ **검증 워커 Codex usage-limit 사망 → 페이블 직접 적대 검증**(p44-verification 2파일 14 tests): 공짜 금지·결정론/격리·경계·매복·저장 위조·ci:sim 2회 바이트 동일 — 7항목 전부 PASS, 프로덕션 결함 0(실패 2건은 검증 픽스처 버그로 판명·수정, 8:08 이후 워커 재검증 예약). ⑥ UI 이벤트 화면(위험/보상 명시·기본 코인 선택 그리드·불가 사유·emoji 금지)+S26(4종 수락/거절/하한 — v5 저장 주입 결정론 14어서션). 검증: 380/380·ci:sim 2회 동일·build·playtest **388**·feedback-check·시각 판정 **93**. engineering-safe / balance-provisional / experience-unverified
- [x] **P4.1~P4.3 결정론 런 그래프·상점 (phase4-graph-shop)** (2026-07-12, Pumasi 구현 1+독립 검증 1+Fable 통합) — ① P4.1 그래프 코어: RunNodeKind 5종·레이어드 노드 리스트, legacyRunGraph 바이트 불변 증명(ci:sim 골든), save v3, 골드 고정 수급(D3, 5전투 175 골든). 통합 감사 3건: payload 계약(combat/elite/boss=encounter 필수+enemies 사전 대조·event=eventId·shop=금지)·`assertRunGraphInvariants` start/settle 공용(settle 직접 호출 우회 차단)·전 레이어 nodeChoices 검사. ② P4.2 선행: 적 행동 원자 3종(applyStatus/heal/**buffNextAttack — 충전/특수 타입 없음(사용자 확정), 버프 의도 표시**)+exhaustive 가드, 몬스터 6종(고블린32·도적26·구울38·마도사22·슬라임16·잿불 마도왕150), CONTENT_VERSION **0.9.0-p4**. ③ P4.2b/P4.3: D9 10레이어 그래프 활성화(`derive(runSeed,'graph')` 전용 스트림, 분기=선택·스킵 없음), 상점 3행동(§6 — 코인 3=`weightedCoinOptions` **공유**·스킬 5=일반3+고급1+무작위1·제거 75+25 누진), save v4 체인(v1→v2→v3→v4), **진행 SSoT=완료 전투 수**(`completedCombatCount` 코어·저장 공유 — 비전투 노드 상한 불증가, 구매 카운터 분리). 독립 검증 워커 판정 7항목 중 High 1건 — victory에서 보스 전투를 스킬 보상 원천으로 오산정(보상 페이즈 없음) → `rewardGrantingCombats` 분리 수정·재현 테스트 green. 통합 결함 수정: db-less 보상 마감이 상점 레이어를 'ready'로 손상시키던 조용한 폴백 → `enterCurrentLayer(db?)` 정확 진입+상점만 소리내어 실패, UI 전 보상 커맨드 db 전달. ④ human telemetry **schema v2**: path 사실(갈림길 선택·상점 행동) UI 기록→리플레이 소비(기록 없는 경로는 mismatch — 임의 정책 금지), v1은 콘텐츠 드리프트와 동일 근거로 거부(실데이터 손실 없음 — 연기 제안 대비 지금 완결로 라이브 로그 무검증화 차단). ⑤ sim: 노드 정책 fight-first/economy-first·상점 구매 정책(D7), seed42 골든 재고정(**fight-first 6전투 패배** — 상점 미사용 기본 정책 관측치, P4.5 경제 MC 판정 대상), run-sim·human 골든 재고정. ⑥ UI: 갈림길·상점 화면(순수 프레젠테이션, 코어 규칙 중복 없음)·HUD 골드·슬롯 픽커, **tofu 글리프 감사 → emoji 의존 전면 제거**. 하네스: S25(상점 구매/제거 누진/비활성/나가기·갈림길 선택 — v4 저장 주입 결정론), S12 그래프 세대 재설계(깊은 노드 보상 UI는 저장 주입 — 브라우저 완주는 승패 비결정), S7/S8/S10 재고정(1전투 시드 롤·`?encounter=raider` 앵커). 검증: 365/365·ci:sim 2회 동일·build·playtest **374**·feedback-check·시각 판정 **92**(1024/1280/1920+RM, tofu 수정 이력 포함). engineering-safe / balance-provisional / experience-unverified
- [x] **P3.4 냉기/전기 캐릭터·보상 가중 함수 (phase3-frost-lightning)** (2026-07-12, Fable 직접 R1+Pumasi R2+Fable R2a/R3, 배포 `8d391f9`·`4c69d92`·`9172f82`+R3) — ① 코인 2종: 냉기(앞면 동상 1)·전기(앞면 감전 1) — P3.1 duration 상태 최초 콘텐츠 탑재. 감사 수정: 상태 proc 대상 라우팅을 **명시 HOSTILE_STATUSES 집합**(burn/frostbite/shock)으로 한정 — self 스킬에 꽂아도 생존 적에게 부여(guard×냉기/전기 회귀 2건), 미래 우호 상태는 skillTarget 유지. ② 캐릭터 2종: 술사(전기 2, 특성 대전된 집중)·냉기 기사(냉기 2, 겨울 외투) + 전용 스킬 각 4종(`exclusiveTo`). 감사 반영: 시작 장착 전용 스킬만으로는 **보상 도달 구조 불가** → 비시작 보상 스킬 3종(과부하·겨울의 손아귀·이지스 서지) R2a 분리 커밋(worker packages-무접촉 게이트 보존). 과부하 골든 12피해 = 기본 감전이 동일 결산 앞면 피해를 증폭(floor(4×1.5)) — 의도 문서화. ③ 보상 가중 함수 §825: `weightedCoinOptions` — 서명 50/기본 30/기타 20/보유 +15(balance-provisional), **풀 ≤3 레거시 exact shuffle 바이트 불변·>3 가중 무복원 3픽**, 시드 결정론+교차 시드 발산+소진 풀 fallback 가중 정본 회귀(감사 3건). testDb ash는 sentinel — 기본 풀 3종 경계 pin 테스트. CONTENT_VERSION **0.8.0-p3.4**+레거시 allowlist 3종, seed42 골든 재고정(HP35·[4,3,4,5,9]) 버전 결속. ④ sim: frost/lightning-build+캐릭터 기본 매핑, 감사 반려 2건 수정 — CRN rewardOffers[0] exact equality 복원(pairedOutcomes 오배치 정정→`assertStreamIsolation`)·frost CRN warrior AA 재사용(false proof)→`runBulkWithAaIdentity` 실증. 증거: 신규 2캐릭터 500런 각 크래시 0·불변식 0, 보상 도달 과부하 183/183·손아귀 180/180·서지 171/171, frost A=A 20쌍 동일, 소표본 바이트 결정론. ⑤ 자산: 카드 아트 11종(**킷 선컴파일 ok=true·0E 후 생성**)+스프라이트 2종(component-row, 행 프롬프트 킷 선검증·Base Lock·Motion QA PASS, dual-tool provenance). ⑥ UI: 동상/감전 키워드 툴팁(카드 행·보너스·칩·티켓 4경로)·frost/lightning 코인 시각 정체성·상태 칩 2종, S24(캐릭터별 부팅·대표 코인·전용 스킬·**정전기장/한파 base 상태 결정론 부여 — 카드 행 한국어 문구·적 칩·티켓 상태 라인 DOM 검증**). 마감 감사 2건: 코인 보상 문구가 frostbite/shock을 "속성 효과"로 뭉개던 결함 → `coin-info.ts` 추출·티켓 `statusKo` 공유(**앞면·동상/감전 +N 명시**, 회귀 5건)·주머니 인스펙터 pop-coin이 fire/mana만 유색 → **정본 element 클래스 렌더**+frost/lightning 그라디언트(트레이 코인 정체성 공유)+S24 팝오버 DOM 검증. 검증: 331/331·ci:sim·골든 green·playtest **379**·feedback-check·시각 판정 93(1024/1280/1920+reduced-motion, 선택 4카드·신규 전투·칩/티켓). engineering-safe / balance-provisional / experience-unverified
- [x] **P3.3 전사 희귀·턴 버프·cost≥2 소비 (phase3-triggers)** (2026-07-12, Pumasi 3라운드+Fable, 배포 `fceb105`·`6d758f9`+R3) — ① 턴 트리거 서브시스템: addTurnTrigger 원자·D6 만료·onDamageDealt(피해 패킷, 0피해 성립, 틱/자해 제외, 재발동 금지)·onAttackSkillResolved(태그 판정). 감사 3건: terminal-first 결정(P5>§12 자구, lethal 골든 fail→pass 증명)·원자별 checkCombatEnd·재귀 lint(중첩 거부). ② 콘텐츠 3종: 화염검(소비1, **utility — attack 태그는 하트 셀프 화상 함정, 구조 lint 'attack⇒적 대상'으로 클래스 차단**+회귀 2건)·불의 심장(소비3 — 첫 count≥2)·대화재(cost5 rare 일회성). §9.4 재계산 `P33_COMBO_BOUNDS.md`. ③ CONTENT_VERSION **0.7.0-p3.3**(0.6.1 정정), 레거시 allowlist(m5·p3.2)+로드 정규화 테스트+fallback 공허 엣지 증명, 골든 재고정([4,3,4,5,9]·HP27)을 버전 승격에 결속. ④ sim: cap500 유지+no-progress 가드(무진행 커맨드 즉시 실패 — cap 완화 은폐 금지), 우선순위 utility 셋업 포함 의도 문서화. ⑤ UI: 턴 버프 칩 **combat.turnTriggers 직접 렌더(SSoT — 이벤트는 연출 전용)**·4경로 툴팁, 결산 트리거 라인은 구간 귀속 오귀속 기각→**고정 매핑+오귀속 회귀**, 카드 트리거 문구 데이터 주도("이번 턴 피해마다 화상 +1" — '특수' 제거), 연료 지정 S22 필수 게이트(3개 제안/교체/비화염 거부/부족/Escape/재진입/확정/소모 3장/만료 각각 DOM 검증)+S23. ⑥ 카드 아트 3종 — **킷 선컴파일 ok=true 후 생성**(P3.2 감사 후속 프로세스 적용), provenance. 검증: 305/305·ci:sim·골든 green·playtest **359**·feedback-check·시각 3뷰포트+reduced-motion·문구/클리핑 DOM 검증. engineering-safe / balance-provisional / experience-unverified
- [x] **P3.2 마나/수호자 빌드 수직 슬라이스 (phase3-guardian-mana)** (2026-07-11, Pumasi 3레인+재작업+Fable) — ① 콘텐츠: 수호자(70HP, 기본 8+마나 2, 특성 고요한 샘 = 전투 시작 임시 마나 1) + 마나 스킬 4종(수호 타격·마나 방벽·방패 반격·마나 샘 — 뒷면 활용 컨셉, 기준표 안). 감시자 기각 반영: 숨김 프로퍼티 풀 경계 해킹 → `SkillDef.exclusiveTo` 명시 데이터 + 존재 비개입 테스트. ② 저장: RUN_SAVE_VERSION 1→2 명시 마이그레이션 + `CONTENT_VERSION` 0.5.0-m5→**0.6.0-p3.2** 승격(레거시 안전 로드·미지 버전 거부), `parsePendingRewards` exclusiveTo 패리티(공용 풀 소진 오판 회귀 수정 — 코어 `rewardEligibleSkillIds` 술어 공유). ③ UI: 캐릭터 선택 화면(순수 신규/`?select=1`만 — 시드 부팅·하네스 불변), `?character=` 직접 부팅, 특성 문구 발동 시점·임시 수명 명시, S21 12어서션. ④ 자산: 수호자 스프라이트 아틀라스(component-row 파이프라인, 모션 QA PASS, 폴백 제거) + 카드 아트 7종(마나 4 + M5 부채 강타/화염 주입/용광로) — prompt-kit 사후 검증 기각 → **정련 프롬프트 11종 전부 ok=true·0 errors** + 비중대 판정 기록, provenance 보존. ⑤ 시뮬: 빌드/보상 정책 축(fire/mana) — 감시자 기각(전 정책 fire 고정 선택) → 재작업 → 잔여 회귀(basic-first가 fire-build로 흡수) 직접 수정: `resolveBuildPolicy` 단일 정본 + 회귀 테스트 2건, **M6 정본 CRN 재현(basic-first 19/20·Δ−2.45)**, guardian mana 1525/1525, 캐릭터 CRN +45.55, guardian 500런 안전 0결함, `p3-balance-report-v1`(증거: `PRD/P32_BALANCE_EVIDENCE.md`, mana-well 사장 플래그는 not-offered로 정정). ⑥ feedback-check 타이머 셰임 결함 수정(거부 칩 1500→250ms 보존 매핑 — 12ms 전역 압축이 칩을 DOM에서 소멸시키던 하네스 결함). 검증: 282/282·ci:sim 4게이트·전투/런 골든 불변·playtest 336·feedback-check passed·시각 판정 95(선택 화면·수호자 전투·카드 아트). 콘텐츠 신규 수치는 기준표 내, 기존 수치 무변경 — engineering-safe / balance-provisional / experience-unverified
- [x] **P3.1 확장 기반 (phase3-foundations)** (2026-07-11, Pumasi 4라운드+Fable, 배포 `427d946`·`f06ec33`·`161183e`+시나리오) — ① `legalCommands` 다중 대상: single-enemy 스킬에 전 생존 적 오름차순 열거, 죽은/범위 밖 target step 거부, 200시드 다중 적 퍼즈 (PHASE3_PREP §1 선행 전제). ② statuses stack/duration 2형 컨테이너: 화상 문법 불변, 동상 가하는 피해 25%↓·감전 받는 피해 50%↑·floor(피해×동상×감전)·화상 틱 수정자 미적용·만료/재부여 가산, 확정 골든 12→9·10→15·동시 13, 이벤트 additive turns 필드 — **콘텐츠는 P3.4까지 미추가** (규칙 기반만). ③ 용광로 chooseBasicInHand 전환(스펙 §3): useFlipSkill chosen 가산 필드+step 검증, 소비 auto-suggest 문법 자동 제안 — seed42 전투·런 골든 모두 불변 확인(재고정 불요). ④ 대상 지정 모드 UI: 생존 ≥2 하이라이트(합법 집합만)·클릭/좌우키+Enter·Escape·기본 대상(마지막 공격 생존→왼쪽 첫 생존), 테스트 전용 `?encounter=duo-raiders`·`?skills=` 진입로(정식 콘텐츠 무접촉). ⑤ 용광로 선택 UI — 연료 지정 문법 재사용, 코인 선택→대상 지정 합성·Escape 역순. 감시자 협업 수정 2건: 폴링 언어 불일치, coinChoiceCommand target-0 고정 결함(첫 적 사망 duo 침묵 실패 → legalCommands 대상 사용+회귀 테스트+코어 chooseBasicCandidates 공개로 UI 규칙 중복 제거). 검증: 257/257·ci:sim 4게이트·전투/런 골든 불변·playtest **324어서션**(신규 S19 타겟팅·S20 용광로 선택 — 감시자 집계 321은 뷰포트 확장 미포함, 실측 [ok] 324)·feedback-check·타겟팅 시각 21항목·시각 판정 94·용광로 선택 모드 시각 확인. CI/Deploy: R1 `29149171275`/`29149171249`, R2 `29149732151`/`29149732180`, R3 `29150216844`/`29150216839` 전부 green. 콘텐츠 수치 무변경 — engineering-safe / experience-unverified
- [x] **P3.0 거버넌스 전환 (phase3-governance)** (2026-07-11, Fable 직접) — 오너 오버라이드(사람 게이트의 개발 차단 효력 해제)를 원장에 기록: `PHASE1_HOLDS.md` 오버라이드 절 + 게이트 행 `experience-unverified`/`waived-for-progression` 재분류(**passed 표기 없음**), `03_PHASES.md` 전제 조건·로드맵 갱신, `PHASE3_PREP.md` 활성화, `VALIDATION.md` Phase 3+ 포인터. 신규 `PHASE3_EVIDENCE_CONTRACT.md` — 3축 라벨(engineering-safe/balance-provisional/experience-unverified), 골든 동결·재고정 절차, 이벤트 additive-first 스키마 정책, 저장 saveVersion 마이그레이션 의무(P3.2부터), CI 매트릭스(sim:balance는 report-only 유지), 지배/사장 옵션·콤보 상한 리포트 의무, 자산 provenance 규칙. 코드 무변경 — 문서만
- [x] **UX 피드백 라운드 — 인과 가독성 (ux-feedback-clarity)** (2026-07-11, Pumasi 3라운드+Fable, 배포 `9758500`) — 사용자 피드백(화상/위축 hover 무반응 · 기본/앞뒤 비직관 · "방금 무슨 일이" 혼란)을 정보·피드백 결함으로 진단하고 **수치·확률·콘텐츠 무변경**으로 수정. ① `keywords.tsx` 7종 용어 툴팁(hover/:focus-visible/클릭·탭 토글, Escape 억제 — WCAG 1.4.13, aria-describedby). ② `card-effects.tsx` 기본/앞면/뒷면/비용 배지 행 — **값 우선 표기**(피해 +3 · 동전마다), any 모드 접미 생략, keep-all 줄바꿈, 클리핑 시 카드 아트 양보(정보>장식, 하한 36px). ③ `resolution-summary.ts`+`resolution-ticket.tsx` 결산 티켓 — 면 칩→기본→발동 보너스(any 1회/per ×N)→상태→이벤트 정본 합계, 다음 커맨드/7초 제거. ④ `vfx.css` 절제 이벤트 VFX(피격·방어·화상·위축·면 확정 금/은 글로우, ≤350ms steps, reduced-motion 전면 비활성, 신규 에셋 0). ⑤ 어휘 정정 쇠약→**위축**. 감시자 협업: 워커의 effect-probe 유령 DOM 셰임 기각→가시 계약 재검증, 슬롯 인덱스 셀렉터 기각→텍스트 파생, 시각 판정 PASS 93 후에도 DOM 계측으로 행 클리핑 2건 적발·수정. 검증: typecheck·lint·test 226/226·build, ci:sim 4게이트·seed42 골든 불변, playtest **293어서션**(신규 S14~S18: 툴팁 4경로·수치 가시 기하·전 카드 클리핑 0·티켓 합계=HP 감소·쇠약 부재·reduced-motion), CI `29139918740`·Deploy `29139918769` green, 라이브 `index-CIEUWhdZ.js` 200 + 신규 문구 포함 확인. 결정 기록 `PRD/UX_FEEDBACK_DECISIONS.md`(변경/불변/보류 3건+진입 기준). 사람 게이트·Phase 3 보류 유지 — 이 라운드는 손맛 게이트를 대체하지 않는다
- [x] **사람 게이트 전 백로그 (pre-human-backlog)** (2026-07-11, Pumasi 3워커+후속 2건+Fable, 배포 `7ea1a17`) — ① 소비 연료 직접 선택 규칙 확정·구현(조건부 보류 닫힘): count 1 = A15 자동 유지, count≥2 = 연료 지정 모드, 실행 coins[] 손패 순서 결정론, 순수 모듈+test-db 테스트. 감시자 지적으로 워커의 ref/DOM 폴백/캡처 중복 통합을 기각하고 최소 경로로 단순화시킨 뒤 수용(제거분 전부 공식 게이트에 불필요 입증). ② 다중 적 엔진 하네스 7테스트 — A7/B17 주장 최초 증거화, 엔진 결함 0, legalCommands target-0은 기준선 한계로 문서화. ③ CI playwright 캐시 + feedback-check 필수 편입 + sim:human 프로세스 레벨 CLI 테스트(`--` 회귀 포함; sim 무의존 원칙 유지 위해 node-shims 확장 — 워커 브리프의 typecheck 게이트 누락은 통합 게이트에서 적발·후속 수정). ④ `PRD/PHASE3_PREP.md` 스펙(타겟팅 UI — legalCommands 다중 target 열거를 진입 필수 전제로 명시, C1 지속형 골든, 용광로 UX, 콘텐츠 게이트 재계산 의무) — 구현은 전부 Phase 3 hold. 검증: typecheck·lint·test 211/211·build, ci:sim 4게이트, seed42 골든 불변, playtest 260어서션, feedback-check, CI `29128783038`·Deploy `29128783037` green, 라이브 `index-Cl5QyWAx.js` 200. content 수치 무변경, 사람 게이트 4건·Phase 3 보류 유지
- [x] **사람 게이트 실행 도구 (human-gate-enablement)** (2026-07-11, Pumasi 3워커+Fable, 배포 `41bab3e`) — ① `pnpm sim:human`: human-run-trace JSON 디렉토리를 **코어 리플레이 대조**(runSeed+커맨드 재시뮬 → HP/플립/결과 사실 일치, contentVersion 드리프트 거부)로 검증하고 §8.3 사람 지표(N·평균 턴·스킬/턴·낭비율·속성 활용률·소비/2코인 기회 정규화·보상 선택·계산 가능 무효 태그)를 결정론 리포트로 집계. sim→core+content 의존 방향 유지, UI import 없음. 후속 수정 2건: pnpm 전달 `--` 토큰 허용, 스킬/턴을 %가 아닌 개수 표기(감시자 발견 `281.6%` → `2.82 (276/98)`). ② 거부 행동 사유 한국어 피드백 + ③ 프리뷰 자해·코인 생성 축 + ④ URL 시드 재현성 S13 + CI playtest 필수 잡 — PHASE1_HOLDS 4행 닫힘(증거 링크 포함). ⑤ `PRD/PLAYTEST_KIT.md` — 테스터 안내·M2/M4/M5/M6 통합 설문·진행자 집계 절차 (Fable 직접 작성). 검증: typecheck·lint·test 195/195·build, ci:sim 4게이트, seed42 골든 불변, playtest 260어서션(로컬+원격 CI), feedback-check, sim:human 2회 byte-identical(결정론 픽스처 N=5 — **도구 검증용이며 사람 증거 아님**). CI/Deploy `29110395064`/`29110395102` green, 라이브 `index-D7PQXBtO.js` 200. 수치 콘텐츠 무변경(T6.4 유지), 사람 게이트·Phase 3 보류 유지
- [x] **M6 정책·지표·CRN·CI·로컬 텔레메트리** (2026-07-11, Pumasi+Fable) — Random/Aggro/Turtle/GreedyEV는 합법 명령과 공개 프리뷰만 사용하며 정책 RNG를 분리했다. `m6-trace-v1` 순수 fold, 정책×적 리포트, A=A 바이트 동일·fire-first/basic-first CRN, 정렬 이상 시드, 결과 화면 `플레이 로그 저장`을 구현했다. CI는 4정책×125=500런의 terminal/crash/invariant/seed42 네 게이트만 사용하고 밸런스는 report-only다. `sim:balance` 2회 11,023 bytes 완전 동일(SHA-256 `af430ad…34cbc`), 500/500 terminal·274승/226패·crash/invariant 0. Fable 판정에 따라 numeric change=none, Gatekeeper 수렴 70건과 소비/플립은 사람 질문으로 유지한다.
- [x] **M5 5연전 런 + 보상 + 저장 + 전투 UI 통합** (2026-07-10, Pumasi) — 독립 reward 스트림의 기본·화염·마나 3종 보상, 코인 제거 취소, 스킬 획득/교체/거절, 풀 소진 시 독립 fallback 코인 보상, attempt salt, HP·주머니·장착 스킬 이월, 콘텐츠 버전 검증형 localStorage 저장/재개를 구현했다. 마나 코인과 주술사·수문장 스프라이트는 gongnyang 프롬프트 검증 후 sprite-gen 파이프라인으로 제작했다. 검증: typecheck 4/4·lint·build 4/4·Vitest 142/142·`sim run --seed 42 --auto` 승리(5전투 4/3/4/4/5턴, 최종 HP 41)·퍼즈 1,000판·Chrome S1~S12 전 시나리오 통과(5 viewport, 콘솔 오류 0)·독립 시각 판정 91점 pass. Fable의 화상/소비 조건부 판정은 사람 검증 대기이며 자동 통과로 세지 않는다.
- [x] **독립 완성도 감사 4차 — 결과 전환·A18 lint·버림/소모 수명주기** (2026-07-10, 배포 `04c7b21` + `e48d113`) — ① 최종 피해/플립 연출 전에 결과 화면이 전장을 덮던 문제를 이벤트 큐·고스트·피해 텍스트가 모두 끝난 뒤 표시하도록 수정, `aria-modal`·기본 포커스·재시작 임시 UI 초기화 추가. ② 문서상 필수였지만 구현되지 않았던 A18 콘텐츠 lint(장전형 1~4, 5는 희귀+일회성/궁극, 6+ 금지, 소비 1~3)를 실제 `contentDb.validate()` 테스트로 결선. ③ 버림·소모 카운터를 클릭 가능한 인스펙터로 변경해 종류/매수·영구/임시·취급 속성·리셔플/복귀/소멸 규칙을 표시하고, 더미 간 상호 배타·Escape/바깥 닫기·78×24px 히트 영역 보장. ④ `coinsDiscarded`·`pileShuffled` 이벤트와 HUD 펄스/이동 문구를 추가하고, 치명타로 전투가 끝나도 장전 비용이 버림으로 정산되도록 수정. 검증: typecheck 4/4·lint·build 4/4·Vitest 62/62·sim seed 42 승리 9턴·퍼즈 1,000판·브라우저 11시나리오 163어서션 전부 통과, HUD 76px 유지, 시각 판정 94점 pass. CI `29082598961`·Deploy `29082599065` 성공, 라이브 번들 `index-LJsdZBv3.js`/`index-DoPuRbqS.css` 및 URL 200 확인
- [x] **독립 감사 3차 — 카드 겹침·승격 상태 분리 (2차 시각 판정 반영)** (2026-07-10, 배포 `7067dd3`) — 사용자 피드백: 장전(.lifted) 카드가 호버와 같은 1.3× 상시 확대를 받아 다중 장전 시 서로를 가림(측정: 두 장전 카드 각 156px·상호 겹침 50px, 1280·1024 동일). 1차 수정(검사 1.18×)은 시각 판정에서 "승격 카드가 여전히 이웃 제목을 가림"으로 revise → 최종: 장전 = 절제된 리프트(-12px)+금테(확대 없음), **검사 승격 = 가로 확대 없는 수직 리프트(-34px)+금테 링+상세 노출**(120px 카드가 상세를 이미 다 담음) — 호버/키보드 포커스/드롭 목적지/해결 중 카드 단 한 장만, 승격 중에도 이웃 겹침이 휴식 수준(14px)이라 이웃 제목·소켓 무가림. `:focus-within`을 `:has(:focus-visible)`로 교체해 마우스 클릭이 남긴 포커스로 승격이 고정되는 부작용 제거(키보드 접근성 유지). 검증: 플레이테스트 S9 강화(1280·1024 — 장전 폭 120·겹침 14px·승격 lift 34px·승격 중 이웃 제목/소켓 히트 테스트·키보드 Tab 승격 동등·겹침 속 소켓 회수) 포함 130어서션 전부 통과, 55/55·lint·typecheck·build ✓. CI `29074068035`·Deploy `29074068037` 성공, 라이브 CSS 확인. 휴식 레일·전장 면적·HUD 76px·캐노니컬 스크린샷 불변
- [x] **독립 감사 2차 — 다중 슬롯 크래시 근본 수정 + 주머니 인스펙터** (2026-07-10, 배포 `2acfd23`) — 사용자 추가 재현 보고 대응. ① **다중 스킬 장전 후 사용 시 화면 소멸의 진짜 근본 원인**: 1차 프리뷰 가드(코스트 완충)로는 부족 — resolveFlip은 턴 3회 캡·재사용·대상 사망에서도 throw하므로, 완충 장전된 카드가 남은 채 3번째 스킬을 쓰면 렌더 크래시. 프리뷰를 "useFlipSkill이 legalCommands에 존재"로 가드(§3.5 preview→null 계약) + 코어 캡 계약 테스트. ② 동전 선택 중 카드 클릭은 장전 전용(장전 불가면 무동작) — 연속 장전 중 스킬 오발 차단. ③ **뽑을 더미 인스펙터**: 주머니 버튼 → 팝오버로 종류·매수 공개(기본/화염/임시 분리 집계, 순서는 시드 비밀이라 비공개), Escape·바깥 클릭 닫기, 좌하단 고정(전장 미가림), 상태 파생이라 드로우·리셔플·턴 전환에 라이브 갱신. ④ 장전 소켓 회수 어포던스(호버/포커스 시 눌림+글로우). 검증: vitest 55/55(신규 4), 플레이테스트 S7 스트레스(부분 장전→다중 장전→회수 보존→해결 후 타 슬롯 유지→캡 상태 완충 렌더 생존→턴 전환→2코인 플립이 피해보다 먼저) + S8 팝오버 전 항목, 5뷰포트 재통과, sim 42 승리 9턴 불변
- [x] **독립 감사 — M2 UI 결함 4건 수정** (2026-07-10, 배포 `2acfd23`) — ① 불타는 일격(cost 2) 1개 장전 시 previewFlip이 코어 검증 예외로 렌더 크래시 → 화면 전체 소멸(재현: 플레이테스트 하네스 S2, 콘솔 "placed coin count must equal skill cost") — 프리뷰를 코스트 완충 시에만 호출하도록 가드 + 코어 계약 테스트 추가. ② 플립 연출이 상태 선반영(코인이 이미 버림 더미) 탓에 가시 대상 없음 — 해결 중 소켓 고스트(.resolving)로 플립·면 공개(앞=금화 양각/뒤=암청 음각) 연출 복원, 면 기록은 coinFlipped/coinsDrawn 이벤트 리듀서로 수명주기 관리(재드로우 시 리셋). ③ M2 T2.3 스펙(드래그 장전·회수)이 클릭 전용으로 구현돼 있었음 — 포인터 드래그(프록시·합법 목적지 하이라이트·무효 드롭 셰이크·소켓 간 이동) + 클릭/키보드 유지 + 문맥 힌트 추가, 판정은 전부 legalCommands/step 위임. ④ 대형 화면에서 1280px 셸 밖 초록 여백 — 풀블리드 배경 + 1280px 콘텐츠 안전폭으로 재구성. 검증: vitest 51/51(신규 14), 플레이테스트 하네스(클릭/키보드/드래그/무효/회수/멀티턴/5뷰포트) 전 항목 통과, sim seed 42 승리 9턴 불변(전투 결과 무변경)

- [x] **M4 소비형 + 취급 태그 + 일회성 (MVP 6종 완성)** (2026-07-10) — 소비형 해결 C0~C4(연료 유효속성 검증·소모 영역 격리·플립 미발동), grantElement(손패 스냅샷, A14), legalCommands 연료 선택(속성 필터 + 취급 우선 A15 — codex의 M2 시절 속성 무시 버그 스펙 지시로 수정됨), 점화 검술·화염 폭주(일회성) 콘텐츠, 전사 6슬롯 완성. 테스트 33/33(콤보·격리·일회성·3회 캡 포함), 퍼즈 1,000판(소비·폭주 경로 포함) ✓
- [x] **M3 화염 코인 + 화상 + 불씨 주머니** (2026-07-10) — 효과 원자 applyStatus/addCoin(뽑을 더미 무작위 삽입·손패 상한 10·임시 플래그), P6 코인별 속성 proc(A8 귀속), 캐릭터 특성 훅(combatStart), 콘텐츠: fire 코인·불타는 일격·점화·전사 basic8+fire2. 골든 8종 포함 테스트 25/25, 퍼즈 1,000판 ✓. 검수에서 잡은 것: sim 퍼즈 하네스의 M1 시절 불변식("생성 없음")이 낡아 원장 불변식(총량=초기+생성)으로 Claude 직접 교체. 전투 종료 임시 코인 정산은 M5 런 도입 시 구현(주석 예약)
- [x] **M2 (코드·시각 검증·배포 완료, 손맛 게이트 대기)** (2026-07-10) — 전투 UI(§15.1 레이아웃, 클릭/키보드/드래그 장전·회수, CSS 3D 플립 연출, 프리뷰 툴팁, 승패 오버레이, URL 시드), core preview.ts(2^n 열거, 순수). 자동 검증과 정식 Pages 배포 완료; 남은 것은 손맛 게이트(사람).
- [x] **M1 헤드리스 전투 코어** (2026-07-10) — content-types 스키마 전체(EffectAtom union, M3/M4 원자는 명시적 예약 에러), CombatState/순수 리듀서/이벤트, 플립 해결 P0~P9(3회 캡 P1 마킹, 1히트 패킷 합산, 패배 우선 판정), 턴 상태기계(T0 리셋·D3 자기 페이즈 방어 리셋·D7 폐기), 약탈자 고정 사이클(초기 의도 공개), CLI play --auto/fuzz, 테스트 14종. codex(신모델) 작성 → Claude 검수(수정: lint 3건, StatusId 확정 어휘 정합). 잔여 과제: attempt 소금 결선(M5), 다중 적 타겟 UI(M5+). `pileShuffled` 이벤트는 `e48d113`에서 해소
- [x] **M0 프로젝트 셋업** (2026-07-10) — pnpm 모노레포(core/content/ui/sim), xoshiro128** 결정론 RNG(계층 derive + attempt 소금), RNG 테스트 7종, ESLint core 순수성 규칙, Vite+React 부트스트랩(base path), sim CLI, GitHub Actions CI. codex 작성 → Claude 검수(수정 2건: CoinUid/SlotId number 브랜드, sim CLI 내로잉) → 검증 통과. 런타임: Node 18(EOL, vitest 구동 불가) → Node 22.14 로컬 설치로 해결

## 마지막 검증 결과

```text
UX 피드백 라운드 후 (2026-07-11, 배포 9758500):
typecheck 4/4 ✓ · lint ✓ · test 25파일 226/226 ✓ · build 4/4 ✓
· ci:sim → 500/500 terminal, crash 0, invariant 0, seed42 golden unchanged ✓
· sim play --seed 42 --auto → victory 9턴 불변 ✓
· playtest 293/293 ✓ (신규 S14~S18: 키워드 툴팁 hover/focus/클릭·탭/Escape/바깥/오발 방지,
  카드 행 문법+수치 가시 기하+전 카드 클리핑 0+폭 ≤126 유지, 결산 티켓 합계=적 HP 감소,
  쇠약 어휘 부재, vfx-reveal 등장·소멸+reduced-motion 무사고)
· 독립 시각 판정 PASS 93/100 + 판정 후 클리핑 2건 DOM 계측 적발·수정·어서션 고정 ✓
· 수치·확률·콘텐츠 무변경 (사람 데이터 전 수치 변경·Phase 3 금지 유지)
· 원격 CI `29139918740`·Deploy `29139918769` green, 라이브 `index-CIEUWhdZ.js`·URL 200 ✓
```

## 실패 시도

| 시도 | 변경 | 결과 | 배운 점 |
| --- | --- | --- | --- |

## 현재 가장 안정적인 상태

**P5.6 최종 마감 `5411969` (1.0.0-rc.1)** — CI `29182741763` 성공 → 신규 게이트 파이프라인(workflow_run exact head_sha) Deploy `29182916941` 성공. 라이브 번들 `index-CUypvaFs.js`에서 `1.0.0-rc.1`·`썩은 육체`(몬스터 패시브) 확인, 타이틀 M0 제거 반영, URL 200. 직전 감사 커밋 `6532867`도 CI `29181798545`→Deploy `29181950305`로 신규 경로 검증 완료.

## 다음 단계

**최신 우선순위(아래 기존 백로그 문단보다 우선):** P8 자동화·시각 회귀는 완료됐다. P9에서 사람 N≥5 플레이로 밸런스·음색·손맛을 판정한다. Fable은 잔여 사용량이 적으므로 방향이 크게 갈리거나 전문 오케스트레이션이 필요한 시점에만 사용한다.

**P6 후속 백로그(비차단)**: 화염 태세/화염 집중 아키타입(지속 스택 상태 계층 필요), 소환 레일 배치 정련·선택 장비 명시 피커 UX, 신규 캐릭터 봇 정책 고도화(격투가 셋업 가치·frost 스톨메이트), guardian 지배 재관측. 그 외 **릴리스 차단 엔지니어링 백로그 없음 — 사람 게이트만 남음.** (비차단 report-only 지표 개선은 열려 있음: TTI·커맨드 라운드트립 지표 재정의 — 연출 포함 측정 유보.) ① 사람 N≥5 플레이테스트(`PLAYTEST_KIT.md`, human trace schema v2 — v1 로그는 거부) → M2 손맛·M4 갈등·M5 수문장 체감·M6 지표 판정. ② 사람 데이터 기반 밸런스 확정 — 현재 열린 관측치: guardian 지배·turtle 정책 우세(48/125 vs aggro 0)·상점 잔여수요 100%·seed42 fight-first 패배. 수치는 전부 balance-provisional, 변경은 결정론 증거가 방향을 특정할 때만. ③ 취향 게이트: BGM/음색·난이도 곡선. ④ 확정 후 v1.0.0 태그 → Release 워크플로(태그 SHA 전체 게이트→Pages→GitHub Release)가 자동 처리. 태그는 사람 판정 후에만 만든다.

## 리스크 / 블로커

- ~~[해소] 감사 수정분 미배포~~ — 커밋 `2acfd23`, CI `29070513959`, Deploy `29070513929` 성공. 라이브 번들에서 주머니 인스펙터·회수 힌트 포함 여부와 HTTP 200 확인.
- ~~[해소] 스크린샷 시스템 라이브러리~~ — sudo 없이 해결 (apt-get download + dpkg -x + LD_LIBRARY_PATH). 시각 검증 통과, Neo둥근모 폰트 정식 탑재
- ~~[해소] 배포처 결정~~ — 리포 공개 전환 후 GitHub Pages 활성화·자동 배포 완료.
- **[experience-unverified] 플레이테스트 게이트 2건**: M2 손맛 · M4 갈등 관찰 — 정식 플레이 URL: https://turnabouthero.github.io/deckbuilding-roguelite/ (2026-07-11 오버라이드로 개발 차단 효력만 해제 — 사람 판정 자체는 여전히 열려 있음)
- **[experience-unverified] M5 수문장·화상 체감 + M6 N≥5 지표 판정** — Fable 검토는 질문 설계 보조일 뿐 사람 판정을 대체하지 않는다. Phase 3 콘텐츠가 늘수록 이 표본의 가치가 커진다.
- **[보고 전용] M6 경고** — 전체 평균 턴 3.761, 속성 활용 0.514, 화상 기여 0.103, Gatekeeper Aggro/Greedy 정확 수렴 70/125. 자동 결함은 아니며 사람 로그로 판정한다.
- 폴리시 백로그(사소): 거부된 행동 사유 피드백, 프리뷰의 자해·코인 생성 축
- 잔여 가정(01_PRD 가정 원장): 사운드 MVP 제외 / 한국어 단일 / 최신 데스크톱 브라우저만
- 코드 작성은 codex exec 위임이 기본 — codex 세션/인증 만료 시 직접 작성 폴백 (PLAN.md 운영 노트)

## 골 정지 보고 — ✅ 해소됨 (2026-07-10, 사용자 결정: 리포 공개 전환 → Pages 활성화 → deploy 워크플로 green → URL 200)

goal-command.md 규약 "요구사항이 충돌하면 자체 수정을 멈추고 사람의 결정을 기다린다" 발동 (2026-07-10).

- **충돌 기준**: VALIDATION.md 완료 기준 매핑 "실제 배포 URL — GitHub Pages 배포 워크플로 + 배포 URL 200 (M2)". PRD가 배포처를 GitHub Pages로 확정(사용자 결정)했으나, 리포가 PRIVATE + 무료 플랜이라 Pages 활성화가 422로 거부됨
- **시도한 해소**: ① Pages API 활성화 시도(422 — 플랜 제약, 재시도 무의미) ② deploy.yml 작성 완료(커밋 보류 — Pages 없이는 실패 워크플로만 쌓임) ③ 대체 접속 수단 제공 — Artifact 단일 파일 빌드 게시(테스터 플레이 가능 URL 확보, 단 "GitHub Pages URL 200"이라는 명시 검증과는 다른 수단이므로 검증 통과로 마크하지 않음 — scope 잠금 "검증 명령 교체 금지" 준수)
- **최종 선택/결과**: 리포 공개 전환 → Pages 활성화 → deploy.yml 커밋 → 정식 URL 200 확인까지 완료.
- **현재 상태**: 배포 차단은 해소됐고 자동 검증·시각 검증·라이브 번들 검증까지 통과. 사람 플레이테스트 게이트만 남았다.

## 인수인계 메모

이 PROGRESS.md는 골잡이가 생성했다. 골 실행 중 매 체크포인트마다 갱신된다.

## 골 시작 기록
- 시작 시각: 2026-07-10T02:35:26+09:00
- 사용 CLI: claude_code
- 컴팩트 후 본문 길이: 975자
